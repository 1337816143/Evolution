# Public mirror — do not edit directly

This repository is the generated GitHub Pages mirror for `1337816143/My-Evolution`.

Mandatory instructions for every AI Agent and maintainer:

1. Do not create, edit, delete, or reorganize website content in this repository.
2. Do not treat this repository as a source project.
3. Make every content, design, data, prompt, report, or asset change in the private repository `1337816143/My-Evolution` first.
4. Run `npm ci && npm test` in the private repository.
5. Let `.github/workflows/publish.yml` rebuild the single complete website and mirror it here through the scoped GitHub App.
6. Do not add unique files here; the next mirror deployment will remove them.
7. Never copy private materials, raw evidence, credentials, or the private repository root here.
8. Prompt changes belong in `08-ai-digital-systems/prompts/library/` in the private repository or must use its `prompt-upsert` interface. Never edit `data/prompts.json` here.
9. Relationship pages must follow the encrypted/public-summary rules from the private repository.

The only files maintained directly here are GitHub Pages infrastructure under `.github/`.

When asked to update this repository, stop and redirect the task to `1337816143/My-Evolution`.
